# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_ALLOWED_HOSTS=api.localhost,localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=your-domain.com,api.your-domain.com,files.your-domain.com
CSRF_TRUSTED_ORIGINS=api.your-domain.com,your-domain.com,
DEBUG=True
USE_COOKIE_DOMAIN_MIDDLEWARE=False # set to false when on localhost
COOKIE_DOMAIN=.your-domain.com
#APP_DOMAIN= # comment out when on localhost

# Backend Settings
BACKEND_PORT=8000 
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=change-me

# Database Settings
DB_NAME=krit_gis
DB_USER=db_user
DB_PASSWORD=change-me
#DB_HOST=krit_gis_db
#DB_PORT=5432
# Local
DB_HOST=localhost
DB_PORT=5432

# Geometry Settings
DEFAULT_SRID=25832

# WebDAV File Access Settings
# Desktop users can mount network drives using Django credentials
# Example: https://files.localhost or https://files.your-domain.com

# Caddy Settings
CADDY_PORT=80
CADDY_HTTPS_PORT=443

# Nginx Settings
NGINX_PORT=80

# GeoServer Settings
GEOSERVER_PORT=8081
GEOSERVER_ADMIN_USER=admin
GEOSERVER_ADMIN_PASSWORD=change-me

# QGIS
QGIS_SERVER_PORT=8082
QGIS_PROJECT_FILE=/projects/default.qgs

# Domain Configuration (for production)
# Uncomment and configure these for production deployment
#DOMAIN_NAME=your-domain.com
#API_DOMAIN=api.your-domain.com
#APP_DOMAIN=app.your-domain.com
#FILES_DOMAIN=files.your-domain.com
#GEOSERVER_DOMAIN=geoserver.your-domain.com
#QGIS_DOMAIN=qgis.your-domain.com

# For local development
DOMAIN_NAME=localhost
API_DOMAIN=api.localhost
APP_DOMAIN=app.localhost
FILES_DOMAIN=files.localhost
GEOSERVER_DOMAIN=geoserver.localhost
QGIS_DOMAIN=qgis.localhost